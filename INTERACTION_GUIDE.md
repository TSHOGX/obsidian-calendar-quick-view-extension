# 交互指南 / Interaction Guide

## 🖱️ 点击行为 / Click Behaviors

### 1. 普通点击 / Normal Click

**操作 / Action:**
```
Click on a calendar day
点击日历上的某一天
```

**效果 / Effect:**
```
┌─────────────────────────────────────┐
│ 2025-10-09      [💾 Save]  [✕]     │  ← 弹出编辑器
├─────────────────────────────────────┤
│ ┌─────────────────────────────────┐ │
│ │ # 2025-10-09                    │ │
│ │                                 │ │
│ │ Your diary content here...      │ │  ← 在这里编辑
│ │                                 │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

- ✅ 打开**编辑弹窗** / Opens **edit modal**
- ✅ 在弹窗中编辑内容 / Edit content in modal
- ✅ 点击 "Save" 保存 / Click "Save" to save
- ✅ 保存后自动刷新日历 / Auto-refresh calendar after save

**适用场景 / Use Cases:**
- 快速编辑 / Quick edit
- 添加简短内容 / Add brief content
- 修正错误 / Fix errors

---

### 2. Ctrl/Cmd + 点击 / Ctrl/Cmd + Click

**操作 / Action:**
```
Windows/Linux: Ctrl + Click
macOS: Cmd + Click
```

**效果 / Effect:**
```
┌─────────────────────────────────────┐
│ Obsidian Editor                     │  ← 在主编辑器中打开
├─────────────────────────────────────┤
│ # 2025-10-09                        │
│                                     │
│ Your diary content...               │  ← 完整编辑器功能
│                                     │
│ [Full editor features]              │
│                                     │
└─────────────────────────────────────┘
```

- ✅ 在 **Obsidian 编辑器**中打开 / Opens in **Obsidian editor**
- ✅ 使用完整的编辑器功能 / Use full editor features
- ✅ 访问所有 Obsidian 快捷键 / Access all Obsidian shortcuts
- ✅ 使用插件（如 Vim mode）/ Use plugins (like Vim mode)

**适用场景 / Use Cases:**
- 长文本编辑 / Long text editing
- 需要高级功能 / Need advanced features
- 习惯使用编辑器 / Prefer editor workflow

---

### 3. 点击空白日期 / Click Empty Day

**操作 / Action:**
```
Click on a day without content
点击没有内容的日期
```

**效果 / Effect:**
```
┌──────────┐
│    9     │  ← 空白日期
│          │
│ Click to │
│ create   │
└──────────┘
         ↓
自动创建新文件 + 打开编辑弹窗
Auto-create file + open edit modal
```

- ✅ 自动创建日记文件 / Auto-create diary file
- ✅ 打开编辑弹窗 / Open edit modal
- ✅ 开始编写新日记 / Start writing new diary

---

### 4. 格子内滚动 / Scroll Within Cell

**操作 / Action:**
```
Hover + Mouse wheel
悬停 + 鼠标滚轮
```

**效果 / Effect:**
```
┌──────────┐
│    9     │  ← 日期
│          │
│ Content  │
│ here...  │
│ ↓↓↓      │  ← 滚动查看
└──────────┘
```

- ✅ 查看完整内容 / View full content
- ✅ 无需打开弹窗 / No need to open modal
- ✅ Markdown 渲染显示 / Markdown rendered display

**适用场景 / Use Cases:**
- 浏览日记 / Browse diary
- 查找特定内容 / Find specific content
- 快速回顾 / Quick review

---

## 📊 对比表格 / Comparison Table

| 操作 | 效果 | 编辑方式 | 功能 |
|------|------|---------|------|
| **Click** | 弹窗编辑器 | Textarea | 基本编辑 |
| **Ctrl/Cmd+Click** | Obsidian 编辑器 | 完整编辑器 | 全部功能 |
| **Scroll** | 查看内容 | 只读 | 浏览 |
| **Click Empty** | 创建 + 编辑 | Textarea | 新建 |

---

## 🎯 推荐工作流 / Recommended Workflow

### 场景 1: 每日记录 / Daily Recording

```
1. 点击今天的日期 (Click today)
   → 打开编辑弹窗
   
2. 快速记录今天的内容
   Quick note of today's activities
   
3. 点击 Save 保存
   Click Save
   
4. 在日历中看到更新
   See update in calendar
```

### 场景 2: 浏览回顾 / Browse & Review

```
1. 在格子内滚动查看每天的内容
   Scroll within cells to view each day
   
2. 看到需要补充的内容
   Found something to add
   
3. 点击格子打开编辑
   Click cell to edit
   
4. 添加内容并保存
   Add content and save
```

### 场景 3: 深度编辑 / Deep Editing

```
1. Ctrl/Cmd+Click 打开编辑器
   Ctrl/Cmd+Click to open editor
   
2. 使用完整编辑器功能
   Use full editor features
   
3. 编辑、格式化、添加链接等
   Edit, format, add links, etc.
   
4. 保存后在日历中查看效果
   Save and view in calendar
```

---

## 💡 使用技巧 / Pro Tips

### Tip 1: 快速编辑 vs 深度编辑

- **弹窗编辑** - 适合快速修改、添加简短内容
- **编辑器** - 适合长文本、需要高级功能时

### Tip 2: 键盘效率

```
Ctrl/Cmd + Click     → 打开编辑器
ESC                  → 关闭弹窗
Click                → 打开弹窗
Scroll               → 浏览内容
```

### Tip 3: 工作流建议

**早上:**
```
Click → 快速记录今日计划
```

**晚上:**
```
Scroll → 回顾今天的记录
Click → 补充内容
Save → 完成
```

**周末回顾:**
```
Scroll through week → 浏览本周
Ctrl+Click → 深度编辑需要完善的内容
```

---

## 🎨 视觉指示 / Visual Indicators

### 格子状态 / Cell States

```
┌────┐  ← 空白 (Empty)
│ 9  │     点击创建
└────┘

┌────┐  ← 有内容 (Has Content)
│ 9  │     点击编辑
│ ↓  │     滚动查看
└────┘

┌────┐  ← 今天 (Today)
│ 9  │     边框高亮
│ ↓  │     
└────┘
```

### 鼠标提示 / Mouse Hints

- 悬停显示"Click to create" - 空白日期
- 悬停显示滚动条 - 有内容的日期
- Cursor 变为 pointer - 可点击

---

## 📝 代码实现 / Code Implementation

```typescript
const handleDayClick = async (
  day: CalendarDay,
  event: React.MouseEvent
) => {
  // 检测 Ctrl/Cmd 键
  const isCtrlClick = event.ctrlKey || event.metaKey;

  if (day.file) {
    if (isCtrlClick) {
      // Ctrl+Click: 打开编辑器
      await app.workspace.getLeaf(false).openFile(day.file);
    } else {
      // 普通 Click: 打开弹窗
      setDiaryModal({
        isOpen: true,
        day: day,
        editableContent: fullContent,
      });
    }
  } else {
    // 创建新文件
    await createDiaryEntry(day.date);
  }
};
```

---

## ✅ 测试清单 / Test Checklist

- [ ] **普通点击** - 打开编辑弹窗
- [ ] **Ctrl+Click (Windows/Linux)** - 打开编辑器
- [ ] **Cmd+Click (macOS)** - 打开编辑器
- [ ] **点击空白** - 创建新文件
- [ ] **格子内滚动** - 查看内容
- [ ] **ESC 键** - 关闭弹窗
- [ ] **点击外部** - 关闭弹窗
- [ ] **保存按钮** - 保存更改

---

## 🎉 总结 / Summary

**三种主要交互方式:**

1. **Click** → 快速编辑弹窗 / Quick edit modal
2. **Ctrl/Cmd+Click** → 完整编辑器 / Full editor
3. **Scroll** → 浏览内容 / Browse content

选择最适合你当前任务的交互方式，提高效率！

Choose the interaction that fits your current task for maximum efficiency!

